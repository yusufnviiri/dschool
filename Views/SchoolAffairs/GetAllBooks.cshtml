@model ICollection<Book> 
<h3>Books</h3>
    <div>
    @foreach(var book in Model)
    {
        <h1 class="text-yellow-700 bg-red-100 ">@book.Title</h1
            <h4>@book.Author</h4>
                        <h4>@book.Category</h4>

    }
</div>